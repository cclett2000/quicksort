// just a class to store variables, will use this method in the future to make variable
// editing easier (avoids having to scour larger programs for variables within each file)

import java.util.ArrayList;

public class variable {
    // file handler
    String PATH = "sample.txt"; // path to sample file
    ArrayList<Integer> data = new ArrayList<>(); // array for file content (after sort)
    ArrayList<Integer> tmp_dat = new ArrayList<>(); //arrayList for file content (before sort)

    // sorting
    int start_ind = 0; // this variable isn't really needed; su
}
